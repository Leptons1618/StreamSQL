# ===================================================================
# StreamSQL Dynamic Configuration
# Supports multiple databases and tables with automatic topic creation
# ===================================================================

# Database 1 Configuration (Source Database)
DB1_HOSTNAME=192.168.1.3
DB1_PORT=1433
DB1_USER=your-username
DB1_PASSWORD=your-password
DB1_NAME=SampleDB
DB1_SERVER_NAME=AXLAP240
# Multiple tables separated by commas - each gets its own topic
DB1_TABLE_INCLUDE_LIST=dbo.Customers,dbo.Products,dbo.Orders
DB1_HISTORY_TOPIC=dbhistory.sql-server-cdc-db1

# Database 2 Configuration (Source Database)
DB2_HOSTNAME=192.168.1.100
DB2_PORT=1433
DB2_USER=your-username
DB2_PASSWORD=your-password
DB2_NAME=SampleDB
DB2_SERVER_NAME=AXLAP235
# Multiple tables separated by commas - each gets its own topic
DB2_TABLE_INCLUDE_LIST=dbo.Customers,dbo.Employees
DB2_HISTORY_TOPIC=dbhistory.sql-server-cdc-db2

# Database 3 Configuration (Optional - add more as needed)
# DB3_HOSTNAME=192.168.1.200
# DB3_PORT=1433
# DB3_USER=your-username
# DB3_PASSWORD=your-password
# DB3_NAME=AnotherDB
# DB3_SERVER_NAME=THIRDSERVER
# DB3_TABLE_INCLUDE_LIST=dbo.Inventory,dbo.Suppliers
# DB3_HISTORY_TOPIC=dbhistory.sql-server-cdc-db3

# Target Database Configuration (where CDC changes are consolidated)
# By default, uses DB1 connection. Override with TARGET_DB_* if needed
# TARGET_DB_HOSTNAME=192.168.1.3
# TARGET_DB_PORT=1433
# TARGET_DB_USER=your-username
# TARGET_DB_PASSWORD=your-password
# TARGET_DB_NAME=SampleDB

# Common Connector Configuration
RECOVERY_POLL_INTERVAL_MS=5000
RECOVERY_ATTEMPTS=4
TASKS_MAX=1
SNAPSHOT_MODE=initial
TOPIC_CREATION_REPLICATION_FACTOR=1
TOPIC_CREATION_PARTITIONS=1
TOPIC_CREATION_ENABLE=true

# Kafka Configuration
KAFKA_BOOTSTRAP_SERVERS=kafka:29092

# CDC Changes Table Configuration
CDC_CHANGES_TABLE=dbo.CDCChanges

# ===================================================================
# TOPIC NAMING CONVENTION
# ===================================================================
# Topics are automatically created with the following pattern:
# {SERVER_NAME}-{TABLE_SAFE}.{SCHEMA}.{TABLE}
#
# Examples for the above configuration:
# - AXLAP240-dbo_Customers.dbo.Customers
# - AXLAP240-dbo_Products.dbo.Products  
# - AXLAP240-dbo_Orders.dbo.Orders
# - AXLAP235-dbo_Customers.dbo.Customers
# - AXLAP235-dbo_Employees.dbo.Employees
#
# Each table gets its own dedicated topic, allowing consumers to
# subscribe to specific data changes they need.
# ===================================================================
